form = document.querySelector(".filter-select")
inputs = form.querySelectorAll("input")
inputs.forEach(input => {
if (input.value == "<%= raw @value %>") {
  input.remove();
}

})

Rails.fire(form, 'submit')

